--// Discord Key UI
--// Mobile + PC
--// Discord: https://discord.gg/bnUmxVt6F

local DiscordInvite = "https://discord.gg/bnUmxVt6F"

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local Player = Players.LocalPlayer

--// GUI
local Gui = Instance.new("ScreenGui")
Gui.Name = "DiscordKeyUI"
Gui.ResetOnSpawn = false
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Gui.Parent = Player:WaitForChild("PlayerGui")

--// Main
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 340, 0, 230)
Main.Position = UDim2.new(0.5, -170, 0.5, -115)
Main.BackgroundColor3 = Color3.fromRGB(12, 12, 20)
Main.BorderSizePixel = 0
Main.Parent = Gui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 14)
Corner.Parent = Main

local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(80, 80, 120)
Stroke.Thickness = 1.5
Stroke.Transparency = 0.25
Stroke.Parent = Main

--// Falling stars background
local Stars = Instance.new("Frame")
Stars.Size = UDim2.fromScale(1, 1)
Stars.BackgroundTransparency = 1
Stars.ClipsDescendants = true
Stars.Parent = Main

for i = 1, 28 do
    local Star = Instance.new("Frame")
    local Size = math.random(1, 3)

    Star.Size = UDim2.new(0, Size, 0, Size)
    Star.Position = UDim2.new(math.random(), 0, math.random(), 0)
    Star.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Star.BackgroundTransparency = math.random(20, 65) / 100
    Star.BorderSizePixel = 0
    Star.Parent = Stars

    local StarCorner = Instance.new("UICorner")
    StarCorner.CornerRadius = UDim.new(1, 0)
    StarCorner.Parent = Star

    task.spawn(function()
        while Star.Parent do
            local Duration = math.random(25, 50) / 10

            Star.Position = UDim2.new(
                math.random(),
                0,
                -0.1,
                0
            )

            local Tween = TweenService:Create(
                Star,
                TweenInfo.new(Duration, Enum.EasingStyle.Linear),
                {
                    Position = UDim2.new(
                        Star.Position.X.Scale,
                        0,
                        1.1,
                        0
                    )
                }
            )

            Tween:Play()
            Tween.Completed:Wait()
        end
    end)
end

--// Content sits above stars
local Content = Instance.new("Frame")
Content.Size = UDim2.fromScale(1, 1)
Content.BackgroundTransparency = 1
Content.Parent = Main

--// Key icon
local Key = Instance.new("TextLabel")
Key.Size = UDim2.new(0, 45, 0, 45)
Key.Position = UDim2.new(0.5, -22, 0, 18)
Key.BackgroundTransparency = 1
Key.Text = "🔑"
Key.TextSize = 31
Key.Parent = Content

--// Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -30, 0, 30)
Title.Position = UDim2.new(0, 15, 0, 60)
Title.BackgroundTransparency = 1
Title.Text = "SCRIPT KEY"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 21
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Parent = Content

--// Description
local Description = Instance.new("TextLabel")
Description.Size = UDim2.new(1, -40, 0, 42)
Description.Position = UDim2.new(0, 20, 0, 91)
Description.BackgroundTransparency = 1
Description.Text = "Join the Discord for scripts,\nupdates and more."
Description.Font = Enum.Font.Gotham
Description.TextSize = 13
Description.TextColor3 = Color3.fromRGB(170, 170, 185)
Description.TextWrapped = true
Description.Parent = Content

--// Copy button
local CopyButton = Instance.new("TextButton")
CopyButton.Size = UDim2.new(0, 280, 0, 38)
CopyButton.Position = UDim2.new(0.5, -140, 0, 142)
CopyButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
CopyButton.BorderSizePixel = 0
CopyButton.Text = "COPY DISCORD"
CopyButton.Font = Enum.Font.GothamBold
CopyButton.TextSize = 13
CopyButton.TextColor3 = Color3.fromRGB(15, 15, 20)
CopyButton.AutoButtonColor = false
CopyButton.Parent = Content

local CopyCorner = Instance.new("UICorner")
CopyCorner.CornerRadius = UDim.new(0, 9)
CopyCorner.Parent = CopyButton

--// Open button
local OpenButton = Instance.new("TextButton")
OpenButton.Size = UDim2.new(0, 280, 0, 30)
OpenButton.Position = UDim2.new(0.5, -140, 0, 187)
OpenButton.BackgroundTransparency = 1
OpenButton.Text = "OPEN DISCORD"
OpenButton.Font = Enum.Font.GothamMedium
OpenButton.TextSize = 12
OpenButton.TextColor3 = Color3.fromRGB(150, 150, 170)
OpenButton.AutoButtonColor = false
OpenButton.Parent = Content

--// Copy functionality
CopyButton.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(DiscordInvite)

        CopyButton.Text = "✓ COPIED TO CLIPBOARD"

        TweenService:Create(
            CopyButton,
            TweenInfo.new(0.2),
            {BackgroundColor3 = Color3.fromRGB(180, 255, 200)}
        ):Play()

        task.wait(1.5)

        CopyButton.Text = "COPY DISCORD"

        TweenService:Create(
            CopyButton,
            TweenInfo.new(0.2),
            {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}
        ):Play()
    else
        CopyButton.Text = "COPY NOT SUPPORTED"

        task.wait(1.5)

        CopyButton.Text = "COPY DISCORD"
    end
end)

--// Open Discord
OpenButton.MouseButton1Click:Connect(function()
    if request then
        request({
            Url = DiscordInvite,
            Method = "GET"
        })
    elseif syn and syn.request then
        syn.request({
            Url = DiscordInvite,
            Method = "GET"
        })
    end
end)

--// Hover effects for PC
CopyButton.MouseEnter:Connect(function()
    TweenService:Create(
        CopyButton,
        TweenInfo.new(0.15),
        {Size = UDim2.new(0, 288, 0, 40)}
    ):Play()
end)

CopyButton.MouseLeave:Connect(function()
    TweenService:Create(
        CopyButton,
        TweenInfo.new(0.15),
        {Size = UDim2.new(0, 280, 0, 38)}
    ):Play()
end)

--// Draggable on PC + mobile
local Dragging = false
local DragStart
local StartPosition

local function UpdateDrag(Input)
    local Delta = Input.Position - DragStart

    Main.Position = UDim2.new(
        StartPosition.X.Scale,
        StartPosition.X.Offset + Delta.X,
        StartPosition.Y.Scale,
        StartPosition.Y.Offset + Delta.Y
    )
end

Main.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.MouseButton1
        or Input.UserInputType == Enum.UserInputType.Touch then

        Dragging = true
        DragStart = Input.Position
        StartPosition = Main.Position

        Input.Changed:Connect(function()
            if Input.UserInputState == Enum.UserInputState.End then
                Dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(Input)
    if Dragging and (
        Input.UserInputType == Enum.UserInputType.MouseMovement
        or Input.UserInputType == Enum.UserInputType.Touch
    ) then
        UpdateDrag(Input)
    end
end)

--// Small entrance animation
Main.Size = UDim2.new(0, 0, 0, 0)

TweenService:Create(
    Main,
    TweenInfo.new(
        0.45,
        Enum.EasingStyle.Back,
        Enum.EasingDirection.Out
    ),
    {
        Size = UDim2.new(0, 340, 0, 230)
    }
):Play()
